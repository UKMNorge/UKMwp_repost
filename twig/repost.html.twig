<h1>Re-publisere {{ post.title|raw }}</h1>

{% if post_status == 'publish' %}
    <p class="lead">Saken er nå publisert!</p>
    <p>
        <a href="../" class="btn btn-primary" target="_blank">Se forsiden</a>
    </p>
    <p> eller </p>
    <p>
        <a href="{{ edit_post_url }}" class="btn btn-primary" target="_blank">Rediger saken</a>
    </p>
{% else %}
    <p class="lead">Saken ligger nå klar som kladd!</p>

    <a href="{{ edit_post_url }}" class="btn btn-primary" target="_blank">Gå til saken for å publisere</a>
{% endif %}